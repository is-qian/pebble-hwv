description: Bosch BMP390 pressure sensor

compatible: "bosch,bmp390"

include: ["sensor-device.yaml", "spi-device.yaml"]

properties:
  int-gpios:
    type: phandle-array
    description: |
      Interrupt output pin
